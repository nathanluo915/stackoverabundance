<div class="comment-block">

  <div class="score-block">
    <%= render partial: 'votes/vote', locals: { votable: comment } %>
  </div>

  <div>
    <h5>Comment: </h5>
    <%= comment.content %>   <pre></pre><%= link_to comment.user.username, user_path(comment.user) %>
  </div>
</div>
